apiVersion: v1
kind: ConfigMap
metadata:
  name: bayesian-config
  namespace: bayesian-assimilation
data:
  application.yml: |
    server:
      port: 8084
    spring:
      jpa:
        hibernate:
          ddl-auto: update
    python:
      service:
        url: http://python-service:8000
  log-level: INFO
