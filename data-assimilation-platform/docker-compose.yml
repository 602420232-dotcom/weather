version: '3.8'

services:
  bayesian-api:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: bayesian-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=INFO
      - ENV=production
    restart: unless-stopped
