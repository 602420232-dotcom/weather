apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-assimilation-service
  namespace: uav-platform
  labels:
    app: data-assimilation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: data-assimilation-service
  template:
    metadata:
      labels:
        app: data-assimilation-service
    spec:
      containers:
        - name: data-assimilation
          image: uav-registry/data-assimilation-service:latest
          ports:
            - containerPort: 8084
              name: http
          env:
            - name: NACOS_SERVER
              value: nacos:8848
            - name: DB_HOST
              value: mysql
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: uav-secrets
                  key: db-password
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            initialDelaySeconds: 20
            periodSeconds: 5
          resources:
            limits: { memory: 512Mi, cpu: 500m }
            requests: { memory: 256Mi, cpu: 200m }
---
apiVersion: v1
kind: Service
metadata:
  name: data-assimilation-service
  namespace: uav-platform
spec:
  type: ClusterIP
  selector:
    app: data-assimilation-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
