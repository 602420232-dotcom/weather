apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-assimilation-service
  namespace: uav-platform
  labels:
    app: data-assimilation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: data-assimilation-service
  template:
    metadata:
      labels:
        app: data-assimilation-service
    spec:
      containers:
        - name: data-assimilation
          image: trae-data-assimilation:latest
          ports:
            - containerPort: 8084
              name: http
          env:
            - name: NACOS_SERVER
              value: nacos:8848
            - name: DB_HOST
              value: mysql
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: uav-secrets
                  key: db-password
            - name: JAVA_OPTS
              value: "-Xms512m -Xmx1g -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+ParallelRefProcEnabled -Djava.security.egd=file:/dev/./urandom"
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            initialDelaySeconds: 20
            periodSeconds: 5
          resources:
            limits: { memory: 2Gi, cpu: 2000m }
            requests: { memory: 1Gi, cpu: 500m }
---
apiVersion: v1
kind: Service
metadata:
  name: data-assimilation-service
  namespace: uav-platform
spec:
  type: ClusterIP
  selector:
    app: data-assimilation-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
