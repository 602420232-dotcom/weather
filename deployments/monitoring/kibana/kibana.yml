server:
  port: 5601
  host: "0.0.0.0"

elasticsearch:
  hosts: ["http://elasticsearch:9200"]
  username: elastic
  password: ${ELASTIC_PASSWORD}

# Kibana logging
logging:
  appenders:
    console:
      type: console
  root:
    appenders: [console]
