[tool.pylance]
# 把 src 目录加入 Pylance 搜索路径
python.analysis.extraPaths = ["./algorithm_core/src"]

[tool.poetry]
# 如果你用 poetry，这里也可以加
name = "bayesian-assimilation"
version = "0.1.0"
packages = [{include = "bayesian_assimilation", from = "algorithm_core/src"}]