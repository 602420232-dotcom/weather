apiVersion: apps/v1
kind: Deployment
metadata:
  name: wrf-processor-service
  namespace: uav-path-planning
  labels:
    app: wrf-processor-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wrf-processor-service
  template:
    metadata:
      labels:
        app: wrf-processor-service
    spec:
      containers:
      - name: wrf-processor-service
        image: wrf-processor-service:latest
        ports:
        - containerPort: 8081
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: WRF_DATA_PATH
          value: "/data/wrf"
        - name: OUTPUT_PATH
          value: "/data/output"
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "2"
            memory: "2Gi"
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 45
          periodSeconds: 15
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 90
          periodSeconds: 45
        volumeMounts:
        - name: wrf-data
          mountPath: /data/wrf
        - name: output-data
          mountPath: /data/output
      volumes:
      - name: wrf-data
        persistentVolumeClaim:
          claimName: wrf-data-pvc
      - name: output-data
        persistentVolumeClaim:
          claimName: output-data-pvc
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
---
apiVersion: v1
kind: Service
metadata:
  name: wrf-processor-service
  namespace: uav-path-planning
spec:
  selector:
    app: wrf-processor-service
  ports:
  - port: 8081
    targetPort: 8081
  type: ClusterIP
