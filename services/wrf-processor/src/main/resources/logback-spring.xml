<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 引用统一的 Logback 基础配置 -->
    <include resource="logback-base.xml"/>

    <!-- Logstash JSON 日志（用于日志采集系统） -->
    <appender name="LOGSTASH" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH:-./logs}/app.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH:-./logs}/app.%d{yyyy-MM-dd}.json.gz</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>{"timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}","level":"%level","thread":"%thread","logger":"%logger","message":"%msg","app_name":"wrf-processor"}%n</pattern>
        </encoder>
    </appender>

    <!-- 为 WRF 处理相关包添加调试日志 -->
    <logger name="com.uav.wrf" level="DEBUG" additivity="false">
        <appender-ref ref="FILE"/>
        <appender-ref ref="LOGSTASH"/>
    </logger>

    <!-- 根日志同时输出到 LOGSTASH -->
    <root level="INFO">
        <appender-ref ref="LOGSTASH"/>
    </root>
</configuration>
