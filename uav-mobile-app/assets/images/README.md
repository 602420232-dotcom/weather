# assets/images

图片资源目录，用于存放应用使用的图片素材（如背景图、Logo、插图等）。

> 当前目录仅包含 `.gitkeep` 占位文件，尚未添加图片资源。

如需使用，将图片放入此目录后，在 `pubspec.yaml` 中注册：

```yaml
flutter:
  assets:
    - assets/images/
```

---
> **最后更新**: 2026-05-14  
> **版本**: 1.0  
> **维护者**: DITHIOTHREITOL
