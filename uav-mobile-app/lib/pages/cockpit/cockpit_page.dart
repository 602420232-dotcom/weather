import 'package:flutter/material.dart';

class CockpitPage extends StatelessWidget {
  const CockpitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('驾驶舱')),
    );
  }
}
