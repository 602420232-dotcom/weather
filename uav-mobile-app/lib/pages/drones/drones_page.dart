import 'package:flutter/material.dart';

class DronesPage extends StatelessWidget {
  const DronesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('无人机管理')),
    );
  }
}
